# ONI Miku Mod v0.1 Release Tidy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prepare the current ONI Miku character mod as a `v0.1.0` internal testing build with Chinese-first documentation and local helper scripts.

**Architecture:** Keep all existing mod content intact and limit changes to repository documentation plus shell scripts under `scripts/`. Verification is done through a lightweight structure check script and manual ONI local mod installation flow on macOS.

**Tech Stack:** Markdown, POSIX shell, git, Oxygen Not Included local mod folder layout

---

### Task 1: Release Documentation

**Files:**
- Modify: `README.md`
- Create: `CHANGELOG.md`
- Create: `ROADMAP.md`
- Create: `NOTICE.md`

- [ ] Document installation, dependency, macOS local path, and test steps in `README.md`.
- [ ] Add a `v0.1.0` internal testing entry in `CHANGELOG.md`.
- [ ] Add staged goals and non-goals in `ROADMAP.md`.
- [ ] Add asset/code separation notice in `NOTICE.md`.

### Task 2: Local Helper Scripts

**Files:**
- Create: `scripts/install_local_macos.sh`
- Create: `scripts/uninstall_local_macos.sh`
- Create: `scripts/check_mod_structure.sh`

- [ ] Add a macOS install script that symlinks `mod_folder` into ONI `mods/Local`.
- [ ] Add a macOS uninstall script that removes only the symlink created for local testing.
- [ ] Add a structure check script that verifies `mod.yaml`, `personalities.json`, and at least one `strings/*.po` file exist.

### Task 3: Verification

**Files:**
- Verify: `README.md`
- Verify: `CHANGELOG.md`
- Verify: `ROADMAP.md`
- Verify: `NOTICE.md`
- Verify: `scripts/install_local_macos.sh`
- Verify: `scripts/uninstall_local_macos.sh`
- Verify: `scripts/check_mod_structure.sh`

- [ ] Run the structure check script and confirm it exits successfully.
- [ ] Review the final diff to ensure no animation, texture, Dupery internals, or `personalities.json` schema changes were introduced.
